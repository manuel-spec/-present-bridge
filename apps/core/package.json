{
  "name": "@packet-bridge/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "smoke": "tsx scripts/smoke-ws.ts"
  },
  "dependencies": {
    "@fastify/websocket": "^11.0.2",
    "@packet-bridge/shared": "workspace:*",
    "bonjour-service": "^1.3.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "mediasoup": "^3.15.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@packet-bridge/tsconfig": "workspace:*",
    "@types/node": "^22.13.4",
    "@types/ws": "^8.5.14",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "ws": "^8.18.0"
  }
}
